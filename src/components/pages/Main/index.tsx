import { Container, Typography } from "@mui/material";

export const Main = () => {
	return (
		<Container>
			<Typography textAlign='center' variant='h1'>
				Main
			</Typography>
		</Container>
	);
};
